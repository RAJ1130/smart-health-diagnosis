services:
  - type: web
    name: mediscan-pro
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn enhanced_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0